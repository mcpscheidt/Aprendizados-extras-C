

void trocar(int* a, int* b) {
	int aux;
	
	aux = *a;
	*a = *b;
    *b = aux;
}

int main() {
	int x = 10;
	int y = 20;
	printf("Antes: x = %d, y = %d\n", x, y);
	trocar(&x, &y);
	printf("Depois: x = %d, y = %d\n", x, y);

	return 0;
}
